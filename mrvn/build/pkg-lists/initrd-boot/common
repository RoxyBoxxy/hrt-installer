#include "base"
# really needed?
#include "kernel"

# Other udebs that are needed for cdrom-boot install
busybox-cvs-udeb
rootskel-locale
languagechooser
discover-data-udeb
discover-udeb
hw-detect
netcfg-static
net-retriever
bogl-bterm-udeb
cdebconf-priority
cdebconf-newt-udeb
usb-discover
choose-mirror

# It is nice to have an editor, in case something goes wrong
nano-udeb

# This is so additional modules (eg, scsi) can be loaded from driver
# floppies.
floppy-retriever
